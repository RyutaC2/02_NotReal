# 運用規約

最終更新：26/5/14　編集者：RyutaC2

<br>

## ブランチ運用
1. GitHub Flowをベースにブランチを詳細に使い分ける運用を行います。
2. 不要になったブランチは即座に削除してください。

### ブランチ命名規則
1. kebab-case（小文字のみ、ハイフン区切り）で命名します。
2. `ブランチタイプ/ユーザ名/内容`でブランチを作成します。詳細は以下のブランチタイプを参照してください。\
（例：`feature/RyutaC2/profile-page`, `fix/TanakaDX/login-logic`）

### ブランチタイプ
- `main`: 本番公開用。常に動作が保証されたコードのみ。
- `develop`: 開発メインライン。ブランチを作成する場合はここから行います。
- `feature/`: 新機能開発。
- `hotfix/`: 重要度が高く、早急な修正が求められるもの。mainからブランチを作成します。マージの際はmainとdevelop両方にマージします。
- `fix/`: 通常の修正。
- `improvement/`: 既存機能の強化、使い勝手の向上。
- `perf/` :パフォーマンス改善。
- `refactor/`: リファクタリング。
- `docs/`: ドキュメント関連。
- `infra/`: インフラ関連。
- `ci/`: CI関連。
- `chore/`: その他、雑務。

※ `hotfix/`の場合のみ`main`から作成します。それ以外の全てのブランチは`develop`から作成します。

<br>

## コミット運用
1. コミットは大前提、一つの意図の編集につき一コミットを徹底してください。

### コミット記述規則
1. 一行目のタイトルは`接頭辞: 内容`で記述します。タイトルの最後に句点を付けないでください。
2. 二行目は空白で改行します。
3. 三行目からは変更点を箇条書きします。
4. 詳細は以下の例や接頭辞タイプを参照してください。
5. 例外として、タイトルで説明が済む、もしくは簡単な変更で内容がほぼ無い場合などは、タイトルのみでも可能です。
6. 破壊的変更を含む場合は接頭辞の後に`!`を記述します。

```
feat: サイドバーを実装

- 画面左側にサイドバーを実装しました。
- サイドバー本体のコンポーネントとして`xxx/zzz/yyy`を実装し、`layout.tsx`のmain要素に追加しました。
- 表示切り替えボタンのコンポーネントは`aaa/bbb/ccc`に実装し、header要素に追加しました。
```
```
imp!: xxxにyyyの判定メソッドを追加

- xxxに新しいメソッドを追加しました。
- zzzで直接aaaの判定結果を得られます。

BREAKING CHANGE: 既存の構造体xxxにおいて、yyyメソッドの実装が必須となります。
これまでの `if xxx.Status == ...` という直接参照は廃止してください。
```

### 接頭辞タイプ
- `feat: `: 新機能開発。
- `hotfix: `: 重要度が高く、早急な修正が求められるもの。
- `fix: `: 通常の修正。
- `imp: `: 既存機能の強化、使い勝手の向上。
- `style: `: CSSなどスタイルの変更。
- `perf: ` :パフォーマンス改善。
- `test: ` :テストコードの追加・修正。
- `refactor: `: リファクタリング。
- `docs: `: ドキュメント関連。
- `infra: `: インフラ関連。
- `ci: `: CI関連。
- `chore: `: その他、雑務。

<br>

## 同期
1. プルリクエストを送る前に最新の`develop`をRebaseしたうえで、コンフリクトの解消と動作確認を済ませてください。
2. 作業中のの同期（取り込み）においても、`develop`からの直接プルやマージは禁止です。Rebaseを利用してください。
3. `-f`（強制プッシュ）オプションは利用しないでください。代わりに、`--force-with-lease`（非破壊強制プッシュ）を利用してください。